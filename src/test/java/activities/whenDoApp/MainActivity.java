package activities.whenDoApp;

import controls.Button;
import org.openqa.selenium.By;

public class MainActivity {
    public Button addButton = new Button(By.xpath("//android.widget.ImageButton[contains(@resource-id,'fab')]"));
    public Button firstTaskLabel = new Button(By.xpath(""));
}
